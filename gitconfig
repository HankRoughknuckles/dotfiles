[user]
	name = Thomas Morris
	email = thomas.imorris@gmail.com

[alias]
	g = grep --break --heading --line-number
  s = status
  com = commit
  b = branch
  c = checkout
  df = diff
  l = log 
  lp = log -p
	lg = log --graph --oneline
	today = !git log --since=midnight --author=\"$(git config user.name)\" --oneline

[color]
	status = auto
	ui = true

[core]
	autocrlf = input
	excludesfile = ~/.gitignore_global
	editor = /usr/bin/vim

[push]
	default = current
